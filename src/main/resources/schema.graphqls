type Training {
  id_training: ID!
  name: String!
}

type Query {
  allTrainings: [Training]
  TrainingById(id_training: ID!): Training
}

type Mutation {
  createTraining(name: String!): Training
}

schema {
  query: Query
  mutation: Mutation
}

